import React from "react";
import { ChevronDownIcon } from "./critical";

export const AngleDownIcon = ({ className, color = "#000" }) => (
  <ChevronDownIcon className={className} color={color} />
);
